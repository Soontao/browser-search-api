{
  "name": "browser-search-api",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test ./test",
    "lint": "eslint src/** --fix",
    "release": "npx standard-version --no-verify && git push --follow-tags origin main",
    "format": "npx prettier -w src"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "@newdash/newdash": "^5.22.1",
    "axios": "^1.7.2",
    "express": "^5.0.0",
    "puppeteer-core": "^24.0.0",
    "turndown": "^7.1.3",
    "undici": "^7.0.0"
  }
}
